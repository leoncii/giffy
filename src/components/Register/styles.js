import styled from 'styled-components'

export const Button = styled.button`
  :disabled {
    opacity: .3;
    pointer-events: none;
  }
`
export const Span = styled.span`
  p {
    color:red;
  }

`